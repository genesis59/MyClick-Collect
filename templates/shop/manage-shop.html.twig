{% extends 'base.html.twig' %}

{% block sidebar %}
<h3 class="text-white">Selectionnez une action:</h3>
    <div class="row">
        <div class="col">
            <a href="{{ path('shop_list-sub-categories',{'id': current_shop.id}) }}" class="btn btn-primary btn-block my-3">Gérer les catégories</a>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <a href="{{ path('shop_list-products',{'id' : current_shop.id }) }}" class="btn btn-primary btn-block mb-3">Gérer les produits</a>
        </div>
    </div>
    <div class="row border-bottom border-info">
        <div class="col">
            <a href="#" class="btn btn-primary btn-block mb-3">Liste des commandes</a>
        </div>
    </div>
{% endblock %}

{% block main %}
    <h1 class="mt-4">Mon magasin {{ current_shop.nameShop }}</h1> 
    {% include 'shop/layout/shopStructureTemplate.html.twig' %}
{% endblock %}